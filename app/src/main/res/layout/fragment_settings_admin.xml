<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
                                       xmlns:tools="http://schemas.android.com/tools"
                                       android:orientation="vertical"
                                       android:layout_width="match_parent"
                                       android:layout_height="match_parent">

    <RelativeLayout android:layout_width="match_parent"
                    android:layout_height="match_parent">

        <TextView
                android:id="@+id/settings_admin_app_version"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal"
                android:paddingBottom="@dimen/spacing_medium"
                android:paddingTop="@dimen/spacing_medium"
                android:textColor="@color/voucherify_text_secondary"
                android:textSize="@dimen/text_size_small"
                tools:text="Version 1.0.0(1234)"/>

        <LinearLayout
                android:id="@+id/settings_admin_logout_item_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"

                android:layout_below="@id/settings_admin_app_version"
                android:layout_marginBottom="@dimen/margin_xlarge"
                android:layout_marginTop="@dimen/margin_medium"
                android:orientation="vertical">

            <Button
                    android:id="@+id/settings_admin_logout_button"
                    style="@style/Voucherify.Base.Button.Accent"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_horizontal"
                    android:minWidth="200dp"
                    android:text="@string/settings_admin_logout_label"/>

        </LinearLayout>
    </RelativeLayout>

</androidx.core.widget.NestedScrollView>